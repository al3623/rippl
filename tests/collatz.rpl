collatz :: int -> [int]
collatz x = 
	let rec_collatz n l =
		if n == 1 
			then 1 pre l
			else if n % 2 == 0
				then rec_collatz (n / 2) (n cons l)
				else rec_collatz (3*n + 1) (n cons l)
	in rec_collatz x []
