%%

let ast_to_string _ = "TEST"

let _ =
	let lexbuf = Lexing.from_channel stdin in
	let expr = Parser.program Scanner.token lexbuf in
	let ast_string = ast_to_string expr in
	print_endline ast_string

