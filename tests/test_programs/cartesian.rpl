toLine = fun x -> fun ylist -> 
	if len ylist == 0
	then []
	else (x,(head ylist)) cons (toLine~x~(tail ylist))

toCartesian = fun xs -> fun ys ->
	if len xs == 0
	then []
	else (toLine~(head xs)~ys) cons (toCartesian~(tail xs)~ys)

main = toCartesian~[1...3]~[1...3]
